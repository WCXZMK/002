# 🎆 烟花绽放网站

一个交互式的烟花表演网站，支持点击发射烟花、背景音乐播放和自动烟花表演。

## 🌟 功能特色

- **交互式烟花**: 点击屏幕任意位置发射烟花
- **自动表演**: 系统自动发射烟花群组表演
- **视觉效果**: 蛇形火箭轨迹、粒子爆炸效果、光晕渲染
- **音效系统**: Web Audio API生成的爆炸音效
- **背景音乐**: 支持本地音乐文件播放
- **响应式设计**: 适配各种屏幕尺寸

## 🚀 快速部署

### 方法1: 静态托管部署
1. 下载所有文件到本地
2. 上传到任何静态网站托管服务：
   - GitHub Pages
   - Netlify
   - Vercel
   - 阿里云OSS
   - 腾讯云COS

### 方法2: 本地服务器
```bash
# 使用Python
python -m http.server 8000

# 或使用Node.js
npx http-server

# 或使用Live Server (VS Code插件)
```

### 方法3: Nginx部署
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/fireworks;
    index index.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
    
    # 音频文件支持
    location ~ \.(mp3|wav|ogg)$ {
        add_header Content-Type audio/mpeg;
    }
    
    # 图片文件缓存
    location ~ \.(jpg|jpeg|png|gif|webp)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

## 📁 文件结构

```
烟花网站/
├── index.html          # 主页面文件
├── style.css           # 样式文件
├── config.js           # 配置文件
├── fireworks.js       # 主应用逻辑
├── entities.js         # 烟花实体类
├── music.js            # 音乐管理器
├── ui.js               # 界面控制器
├── filemanager.js      # 文件管理器
├── 1.jpg               # 背景图片
└── 1.mp3               # 背景音乐
```

## ⚙️ 配置说明

### 音乐配置
在 `index.html` 中可以修改音乐设置：
```javascript
FireworksConfig.music.tracks = [{
    name: '背景音乐名称',
    url: 'your-music.mp3',
    loop: true
}];
FireworksConfig.music.volume = 0.4; // 音量0-1
```

### 自动发射配置
在 `fireworks.js` 中可以调整自动发射频率：
```javascript
this.autoLaunchInterval = 400; // 自动发射间隔(毫秒)
```

### 视觉效果配置
在 `entities.js` 中可以调整烟花效果：
```javascript
const count = 80 + Math.random() * 40; // 爆炸粒子数量
const speed = Math.random() * 1.5 + 0.5; // 粒子速度
```

## 🌐 浏览器兼容性

- ✅ Chrome 60+
- ✅ Firefox 55+
- ✅ Safari 12+
- ✅ Edge 79+
- ✅ 移动端浏览器

## 🔧 技术栈

- **HTML5 Canvas**: 图形渲染
- **Web Audio API**: 音效生成
- **ES6 Classes**: 面向对象编程
- **CSS3**: 样式和动画
- **Vanilla JavaScript**: 纯JavaScript实现

## 📱 移动端优化

- 支持触摸事件
- 响应式布局
- 性能优化
- 触摸友好的交互设计

## 🎨 自定义主题

可以通过修改以下文件来自定义外观：
- `style.css`: 修改颜色、字体、动画
- `config.js`: 调整文件类型支持
- `fireworks.js`: 修改烟花行为逻辑

## 📄 许可证

MIT License - 可自由使用和修改

## 🤝 贡献

欢迎提交Issue和Pull Request来改进项目！

---

**享受绚烂的烟花表演吧！🎆✨**